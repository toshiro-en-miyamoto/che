#!/bin/bash
echo "Removing your Che configuration and destroy user projects and database"
docker run -it --rm \
  --volume=/var/run/docker.sock:/var/run/docker.sock \
  --volume=`pwd`/data:/data \
  --env=CHE_HOST=10.249.26.199 \
  eclipse/che destroy
