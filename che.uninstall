#!/bin/bash
echo "Deleting Che's images from your Docker registory"
docker run -it --rm \
  --volume=/var/run/docker.sock:/var/run/docker.sock \
  --volume=`pwd`/data:/data \
  --env=CHE_HOST=10.249.26.199 \
  eclipse/che rmi
docker rmi -f eclipse/che
